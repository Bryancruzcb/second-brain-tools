chromadb==1.5.9
rank_bm25==0.2.2
sentence-transformers==5.6.0
fastapi==0.139.0
uvicorn==0.51.0
httpx>=0.27
pydantic==2.13.4
python-dotenv==1.2.2
# main.py imports both at module scope. They arrived transitively through
# sentence-transformers, which is not a contract -- a dependency drop there
# breaks the container on first import.
numpy==2.4.6
# retrieval.OnnxCrossEncoder serves RERANKER_ONNX_FILE through onnxruntime.
# chromadb already depends on it; the pin makes that a contract.
onnxruntime==1.27.0
scikit-learn==1.9.0
