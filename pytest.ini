[pytest]
# Keep test output pristine. The one filtered warning is upstream noise:
# starlette deprecates plain-httpx TestClient in favor of httpx2, and the
# warning fires on `import fastapi.testclient` regardless of our code.
# Revisit when the fastapi/starlette pins next move (see requirements.txt).
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*:Warning:fastapi.testclient
