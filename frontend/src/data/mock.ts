/** Legacy demo fixtures — Night Atlas panels now load from the live FastAPI backend via `@/lib/api`. Kept for reference / offline screenshots only. */

export const productName = "Atlas";
export const productTagline = "Atlas";
